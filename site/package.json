{
  "name": "terence-world-site",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "validate:links": "node scripts/validate_links.mjs",
    "rag:build": "node scripts/build_rag_index.mjs",
    "prebuild": "npm run validate:links && npm run rag:build",
    "build": "astro build",
    "preview": "astro preview",
    "test:e2e": "playwright test"
  },
  "dependencies": {
    "@astrojs/vercel": "^9.0.4",
    "astro": "^5.16.11",
    "openai": "^4.62.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.51.1"
  }
}
